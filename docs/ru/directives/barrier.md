Синхронизирует все потоки в группе; все потоки приостанавливаются на барьере до тех пор, пока все потоки не достигнут барьера.

```cpp
#pragma omp barrier
```

### Примечания

Директива `barrier` не поддерживает клаузы.